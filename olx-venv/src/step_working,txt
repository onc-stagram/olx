--sec2
--create venv 
    python -m venv "olx-venv"
--activate use Git Bash
    source ./olx-venv/Scripts/activate
--create folder src in olx-venv
--install django with
    pip install django

    pip freeze (check dependecies)


--create project in src
cd src
    django-admin startproject olx
--check file
    ls
--create emmpy database
    python manage.py runserver 
--create table
python manage.py migrate

sec4==========================
--create folder product 
 python manage.py startapp product

--edit setting.py under folder olx
    INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'product',
]

-- create models.py in product 
create table in database 
item:
    -title
    -images
    -price
    -condition
    -item type-category
    -description
    -price
ad :
    -date& time
    -ad id
    -ad description
owner:
    -city
    -phone number
feeback:
    contact

function:
    -login - register -forget pass
    -post add
    -filtter
    -search

==============================================================
--create models product for create database
class Product(models.Model):
    CONDITION_TYPE = (
        ("New", "New"),
        ("Used", "Used")
    )

    name = models.CharField(max_length=100)
    description = models.TextField(max_length=500)
    condition = models.CharField(max_length=100, choices=CONDITION_TYPE)
    price = models.DecimalField(max_digits=10, decimal_places=5)
    created = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return self.name
===============================================================
--crate table product
    python manage.py makemigratins
    python manage.py migrate

    
